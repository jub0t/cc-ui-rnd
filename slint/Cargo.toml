[package]
name = "wolfcut-ui"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
slint = "1.14"

[build-dependencies]
slint-build = "1.14"

[profile.release]
opt-level = "s"
lto = true
